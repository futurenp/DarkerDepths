modLoader = "javafml"
loaderVersion = "[32,)"
license = "All rights reserved"

[[mods]]
    modId="darkerdepths" #mandatory
    version="1.1.2" #mandatory
    displayName="Darker Depths" #mandatory
    logoFile="ddicon.png" #optional
    authors="Naterbobber" #optional
    description='''A mod all about expanding vanilla caves'''

[[dependencies.darkerdepths]] #optional
    # the modid of the dependency
    modId = "forge" #mandatory
    # Does this dependency have to exist - if not, ordering below must be specified
    mandatory = true #mandatory
    # The version range of the dependency
    versionRange="[34.1.42,)"
    # An ordering relationship for the dependency - BEFORE or AFTER required if the relationship is not mandatory
    ordering = "NONE"
    # Side this dependency is applied on - BOTH, CLIENT or SERVER
    side = "BOTH"
    # Here's another dependency

[[dependencies.darkerdepths]]
    modId = "minecraft"
    mandatory = true
    # This version range declares a minimum of the current minecraft version up to but not including the next major version
    versionRange = "[1.16.3,1.17)"
    ordering = "NONE"
    side = "BOTH"

#[[dependencies.darkerdepths]]
#    modId = "cavebiomeapi"
#    mandatory = true
#    versionRange="[1,)"
#    ordering="NONE"
#    side="BOTH"